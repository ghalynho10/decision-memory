[project]
name = "starter-adapter"
version = "0.1.0"
description = "A minimal teaching adapter for decision-memory: reads a tiny neutral Markdown format under decisions/."
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["uv_build>=0.11.32,<0.12.0"]
build-backend = "uv_build"
